"use client";

import { ParentDocuments } from "@/components/dashboard/parent/ParentDocuments";

/** Full library of PDFs/files in one place. Timetable, Holidays, and Preschool policies also have their own sidebar pages with only that content. */
export default function ParentDocumentsLibraryPage() {
    return <ParentDocuments />;
}
